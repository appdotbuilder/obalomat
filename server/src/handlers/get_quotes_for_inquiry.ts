import { type Quote } from '../schema';

export async function getQuotesForInquiry(inquiryId: number): Promise<Quote[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all quotes received for a specific inquiry.
    // Should include supplier information for each quote to help buyer compare offers.
    return Promise.resolve([]);
}